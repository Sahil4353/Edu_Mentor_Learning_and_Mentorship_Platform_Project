<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:hardwareAccelerated="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EduMentorLearningAndMentorshipPlatformProject"
        tools:targetApi="31">

        <!-- Set LoginActivity as the launcher activity -->
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Other Activities -->
        <activity
            android:name=".activities.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        <activity
            android:name=".activities.DashboardActivity"
            android:exported="false" />
        <activity
            android:name=".activities.VideoPlayerActivity"
            android:hardwareAccelerated="true"/>
        <activity android:name=".activities.BookSessionActivity" />
        <activity android:name=".activities.PaymentActivity" />
        <activity android:name=".activities.ThankYouActivity" />
        <activity android:name=".activities.MentorDashboardActivity" />
        <activity android:name=".activities.MentorMyCoursesActivity" />
        <activity android:name=".activities.UpcomingSessionsActivity" />
        <activity android:name=".activities.NotificationsActivity" />
        <activity android:name=".activities.AdminDashboardActivity" />
        <activity android:name=".activities.AdminUsersActivity" />
        <activity android:name=".activities.EditUserActivity" />
        <activity android:name=".activities.AdminCoursesActivity" />
        <activity android:name=".activities.CreateCourseActivity"/>


        />

    </application>

</manifest>
